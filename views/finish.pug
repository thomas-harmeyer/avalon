extends layout

block content
    .container-fluid
        .row.col.justify-content-center
            if result=="win"
                h1 Congrats you won
            else
                h1 Yikes you lost
        .row.col.justify-content-center
            a.btn.btn-primary(href="/") Play Again
